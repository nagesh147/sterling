[pytest]
asyncio_mode = auto
asyncio_default_fixture_loop_scope = function
testpaths = tests
markers =
    directional: directional-engine parity and behavior tests
    kite: Kite-engine parity and behavior tests
