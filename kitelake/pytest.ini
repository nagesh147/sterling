# Run with:  ./.venv-kitelake/bin/python -m pytest kitelake/tests -c kitelake/pytest.ini
# Kept out of the repo root so it cannot collide with the backend's pytest configuration.
[pytest]
asyncio_mode = auto
